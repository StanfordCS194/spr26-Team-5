# Backend

FastAPI backend for the iPhone face recognition prototype.

## Run

```bash
UV_PROJECT_ENVIRONMENT=.uv-venv uv run --python 3.11 --extra real-recognition uvicorn app.main:app --host 0.0.0.0 --port 8000
```
