# Backend

FastAPI backend for the iPhone face recognition prototype.

## Run

```bash
UV_PROJECT_ENVIRONMENT=.uv-venv uv run --python 3.11 --extra real-recognition uvicorn app.main:app --host 127.0.0.1 --port 8000
```
